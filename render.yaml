services:
  - type: web
    name: soulsync-connect
    env: node
    rootDirectory: .
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production